The calls to `multiplier` can be optimized since `multiplier(int1, 0)` will always return 0 and `multiplier(int2, 1)` will return `int2`. Additionally, multiplying `double2` by 0.0 will always result in 0.0, so this can be optimized away.